public interface IBlastable
{

    //For now we don't need an interface because we have only 1 type of block and they have a basic blast
    //But in the future we might use it for skills so i make it as an interface
    public void Blast();
}
